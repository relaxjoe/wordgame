<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wordle-Like Game</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <!-- email and the game grid -->
    <div class="container mx-auto px-4 py-8">
        <!-- User Email Display -->
        <div class="flex justify-end">
            <span class="text-sm text-gray-700">User: {{userEmail}}</span>
        </div>
        
        <!-- Game Grid -->
        <div class="mt-8 flex justify-center items-center">
            <div class="grid grid-cols-4 gap-4">
                <!-- 4x6 grid for the game -->
                {{#each rows as |row rowIndex|}}
                <div class="flex justify-between space-x-1">
                    {{#each row as |cell cellIndex|}}
                    <div class="w-16 h-16 bg-white border border-gray-300 flex justify-center items-center text-lg font-bold uppercase">
                        {{cell}}
                    </div>
                    {{/each}}
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</body>
</html>
